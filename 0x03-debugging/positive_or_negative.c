#include <stdio.h>

/**
 * main - Test a function tat print if a given input is negative or positive
 * Return: Always 0 (Success)
 */

int main(void)
{
int i;
i = 0;
positive_or_negative(i);
return (0);
}

